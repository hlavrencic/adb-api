version: '3.8'

services:
  adb-api:
    build: .
    container_name: adb-control-api
    ports:
      - "8000:8000"
    volumes:
      - /tmp/adb_screenshots:/tmp/screenshots
    network_mode: host
    restart: unless-stopped
    environment:
      - TZ=America/Argentina/Buenos_Aires
